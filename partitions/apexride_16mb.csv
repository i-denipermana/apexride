# Partition table for the ESP32-S3 N16R8 (16 MB flash).
#
# Ride storage is the binding constraint on this device: at the V1 log rate a
# ride costs roughly 4.3 MB/hour, so the filesystem gets everything that is not
# needed for the application.
#
# There is deliberately no second OTA slot. Over-the-air update is not a V1
# requirement, and reserving a 3 MB mirror would cost around 40 minutes of
# recording time. Adding OTA later means shrinking the littlefs partition,
# which reformats it — so unsynced rides must be downloaded first.
#
# Name,     Type, SubType,  Offset,   Size,     Flags
nvs,        data, nvs,      0x9000,   0x5000,
phy_init,   data, phy,      0xE000,   0x1000,
factory,    app,  factory,  0x10000,  0x300000,
ridefs,     data, spiffs,   0x310000, 0xCE0000,
coredump,   data, coredump, 0xFF0000, 0x10000,
